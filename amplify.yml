# AWS Amplify Hosting build spec.
#
# Amplify clones this repo on every push to a watched branch, runs the
# phases below, and serves whatever lands in `artifacts.baseDirectory`
# from its global CDN.
#
# This is a HISTORICAL reference book — content won't change often.
# changefreq in the sitemap is "yearly", and pushes here are expected
# to be rare (typos, broken-link fixes, refreshed framing — not
# substantive updates, since the language is retired).

version: 1
frontend:
  phases:
    preBuild:
      commands:
        - export MDBOOK_VERSION="0.4.40"
        - curl -fsSL "https://github.com/rust-lang/mdBook/releases/download/v${MDBOOK_VERSION}/mdbook-v${MDBOOK_VERSION}-x86_64-unknown-linux-gnu.tar.gz" | tar -xz -C /tmp
        - /tmp/mdbook --version
        # Regenerate sitemap.xml from SUMMARY.md on every deploy so it
        # stays in sync without manual intervention.
        - node scripts/build-sitemap.mjs
    build:
      commands:
        - /tmp/mdbook build
  artifacts:
    baseDirectory: book
    files:
      - '**/*'
