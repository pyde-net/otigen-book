# Cross-contract calls and interfaces

> **Status:** stub — to be written.

The `interface` declaration, typed call handles via `Interface::at(address)`, and the `raw_call!` low-level escape hatch. Payable cross-calls. Return-data decoding. Failure modes — what `raw_call!` returns when the callee reverts, and how to surface a typed error across a contract boundary.
