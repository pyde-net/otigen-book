# Decoding revert data

> **Status:** stub — to be written.

How a caller pattern-matches an error returned from a cross-contract call. The ABI layout of revert data. Round-tripping a typed error across a contract boundary.
