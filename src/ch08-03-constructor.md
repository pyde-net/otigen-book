# `#[constructor]`

> **Status:** stub — to be written.

One-shot deployment-time initialisation. Why the constructor has no selector. Allowed and forbidden combinations with other attributes. The `Contract::new(...)` interface tests and scripts use.
