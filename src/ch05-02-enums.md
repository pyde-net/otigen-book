# Enums and variants

> **Status:** stub — to be written.

The `enum Name { Variant1, Variant2, ... }` syntax. Variant values, discriminants, and the default-variant rule. When to reach for an enum vs a `u8` flag.
