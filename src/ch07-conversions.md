# Conversions and casts

> **Status:** stub — to be written.

The `as` cast — widening as a no-op, narrowing as a checked operation that may revert. Conversions between `Address` and `u256`. When the type system inserts conversions automatically and when it makes you write them.
