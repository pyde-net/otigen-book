# Primitive types

> **Status:** stub — to be written.

The integer ladder (`u8`–`u256`, `i8`–`i256`), `bool`, `Address`, `String`, and `bytes`. Width semantics, zero values, literal forms, and the few places where the language picks a default for you.
