# Appendix D — Common compiler errors

> **Status:** stub — to be written.

A catalogue of the most-frequent compile errors, organised by what the user was probably trying to do. Each entry has the error message, a minimal example that produces it, and the smallest fix.
