# Appendix F — Tooling

> **Status:** stub — to be written.

The CLI reference for `otic` and `pyde-dev`. The `pyde.toml` schema. IDE setup for syntax highlighting and language-server integration. The package manager `pyde install` and the lockfile format.
