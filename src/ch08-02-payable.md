# `#[payable]` and `msg.value`

> **Status:** stub — to be written.

The payable attribute, value-receiving functions, and the `msg.value` gate. Why non-payable functions cannot even read `msg.value`. Idioms for refunding overpayment.
