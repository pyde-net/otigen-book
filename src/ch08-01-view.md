# `#[view]` and view purity

> **Status:** stub — to be written.

The view-purity static analysis. What a `#[view]` function may and may not do. Transitive checking through internal calls. When a getter shouldn't be a view.
