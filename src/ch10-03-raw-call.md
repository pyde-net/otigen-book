# `raw_call!`

> **Status:** stub — to be written.

The low-level call macro. Manual calldata encoding. The `(success, return_data)` tuple. Use cases (forwarder contracts, dynamic dispatch) and the safety trade-offs.
