# Appendix C — Built-in functions and globals

> **Status:** stub — to be written.

The complete list of `block.X`, `msg.X`, `tx.X`, `Address::X`, `gas_remaining()`, and every built-in macro. One paragraph per item.
