# `#[receive]` and `#[fallback]`

> **Status:** stub — to be written.

Bare value transfers and unknown-selector dispatch. The required shapes for each. When a contract needs them and when it doesn't.
