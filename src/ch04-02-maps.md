# Maps and nested maps

> **Status:** stub — to be written.

The `Map<K, V>` type in depth. Allowed key and value types. Nested map syntax (`Map<A, Map<B, C>>`) and read/write through chained indexing. Why maps live only in storage. The "keep a `Vec<K>` next to your map" pattern for enumerating keys.
