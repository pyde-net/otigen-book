# Events and logs

> **Status:** stub — to be written.

The `event` declaration syntax, `#[indexed]` topics, the three-topic limit, and the `emit` statement. The wire format of a receipt entry and what an indexer sees. Anti-patterns: events used as state, events used as cross-call signalling.
