# Checked arithmetic

> **Status:** stub — to be written.

Why all Otigen arithmetic is checked. The cost of the checks. Idioms for when wrapping is actually wanted (bit-masking, modular arithmetic in a primitive). The gas-cost table for arithmetic ops on each integer width.
