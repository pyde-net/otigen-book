# The `match` control flow

> **Status:** stub — to be written.

The `match` keyword. Exhaustiveness checking. Wildcard patterns. Using `match` as an expression. Pattern matching enum variants vs literal values.
