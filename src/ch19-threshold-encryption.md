# Threshold-encrypted transactions

> **Status:** stub — to be written.

How Pyde's threshold-encryption pipeline protects against MEV. What an encrypted transaction looks like from the contract's point of view (the contract sees plaintext after decryption; the chain ordered the transaction before learning the plaintext). When this matters for contract design (auctions, DEXes, sensitive parameter updates) and when it doesn't (token transfers).
