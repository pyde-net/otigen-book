# Maps and the typed storage block

> **Status:** stub — to be written.

The full story of `Map<K, V>`, nested maps, lazy allocation, and why maps are storage-only. The chapter that explains how the storage block becomes a slot layout, and what 'no inline initialiser' actually means at compile time.
