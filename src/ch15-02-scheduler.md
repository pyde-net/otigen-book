# The scheduler's view

> **Status:** stub — to be written.

What the scheduler sees: a conflict graph over access lists. How it groups transactions into parallel batches. The cost model that drives the grouping.
