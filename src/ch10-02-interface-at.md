# `Interface::at(address)`

> **Status:** stub — to be written.

Obtaining a typed handle to a deployed contract. Calling methods through the handle. Passing native value with a call. What happens when the target contract reverts.
