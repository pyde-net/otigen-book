# `deploy!`

> **Status:** stub — to be written.

Deploying a contract from inside another contract. The constructor-argument encoding. Returning a typed `Contract<T>` handle. Pre-deploying with a known address (CREATE2-style).
