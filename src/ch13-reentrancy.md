# Reentrancy: off by default

> **Status:** stub — to be written.

The default reentrancy guard in detail — the reserved slot, the entry/exit pattern, the gas cost, the cases where it is and isn't sufficient. When `#[reentrant]` is appropriate. The check-effects-interactions discipline as it applies under an auto-guard. A walkthrough of the DAO-style attack and why it cannot happen by default.
