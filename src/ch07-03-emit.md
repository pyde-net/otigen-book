# The `emit` statement

> **Status:** stub — to be written.

Syntax and semantics of `emit`. Idiomatic ordering (state change → emit). Anti-patterns: events used as state, events used for cross-call signalling.
