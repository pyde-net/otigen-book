# Defining structs

> **Status:** stub — to be written.

The `struct Name { ... }` syntax. Field types and ABI representation. Constructing struct values with `Name { field: value, ... }`. Accessing fields with dot notation. Using structs in storage and events.
