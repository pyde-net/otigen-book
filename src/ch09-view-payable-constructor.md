# View, payable, constructor

> **Status:** stub — to be written.

The three most common function attributes in depth. `#[view]` and the static view-purity analysis. `#[payable]` and the `msg.value` gate. `#[constructor]` and one-shot deploy semantics. Why these three are mutually exclusive in the combinations they are.
