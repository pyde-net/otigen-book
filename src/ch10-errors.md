# Errors and reverts

> **Status:** stub — to be written.

Typed errors, the `require!` and `revert!` macros, how revert data is encoded, and how a calling contract pattern-matches an error returned from a cross-contract call. When to use a typed error vs a string.
