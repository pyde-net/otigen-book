# Composite types

> **Status:** stub — to be written.

Fixed arrays (`[T; N]`), dynamic vectors (`Vec<T>`), tuples, user structs, user enums, and the contract handle types `Contract<C>` / `Interface<I>`. How each is laid out, how they pass through ABI boundaries, and where each is the natural choice.
