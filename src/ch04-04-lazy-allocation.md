# Lazy allocation and zero values

> **Status:** stub — to be written.

Why an unread map entry costs nothing. The full zero-value table for every type (integers, bool, Address, String, bytes, struct, enum). Refund-on-zero semantics.
