# The ABI: selectors and JSON

> **Status:** stub — to be written.

The complete ABI format. Selector derivation (FNV-1a, 4 bytes, with the constructor at `0x00000000`). The JSON schema of an `abi.json`. How error and event types are represented. Backwards-compat considerations when changing a function signature.
