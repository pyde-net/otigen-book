# Functions, visibility, entry points

> **Status:** stub — to be written.

The complete function declaration grammar. `pub` vs internal. Selector derivation (FNV-1a, 4 bytes). Parameter passing semantics. Internal calls vs external dispatch — what the compiler does to make in-contract calls to `pub` functions cheap.
